# 动态异构冗余机制真实联调验证报告

生成时间：2026-04-30

## 1. 验证目标

本轮验证目标是确认轻量级动态异构冗余机制是否真实进入模糊测试运行时链路，而不是继续研发新功能。验证范围包括：

- 模糊测试集成适配层 `submit / query / report_query` 的可调用性；
- O2OA profile 中 AE 一阶段与 GAN / rule fallback 二阶段决策是否接入运行时；
- O2OA 与 Flowable profile、runner template、模型与示例请求是否可加载；
- 在本地授权环境中判断是否能够支撑“轻量级动态异构冗余机制已实现”的工程结论。

本报告不包含任何账号、密码、token 或 cookie 明文。

## 2. 环境检查结果

| 项目 | 结果 |
| --- | --- |
| 当前目录 | `/home/dministrator/AFLplusplus`，符合项目根目录要求 |
| Python | `3.12.3` |
| Python executable | `/home/dministrator/AFLplusplus/.venv/bin/python3` |
| `.venv` | 存在，可用 |
| `torch` | 可 import，版本 `2.11.0+cu130` |
| `numpy` | 可 import，版本 `2.4.2` |
| AFL++ 主程序 | `./afl-fuzz` 存在，可执行；版本输出显示 `afl-fuzz++4.36a` |
| Docker | `docker --version` 不可用，当前 WSL 发行版未启用 Docker CLI |
| Docker Compose | `docker compose version` 与 `docker-compose --version` 均不可用 |
| 仓库内 compose 文件 | 未发现 `docker-compose.yml` 或 `compose.yml` |
| O2OA 启动方式 | 仓库文档仅指向外部 Windows Docker Desktop 目录，当前仓库无可直接启动的 compose |
| Flowable 启动方式 | 未发现明确的本地 compose 启动文件 |
| O2OA 服务 | 已完成真实 smoke，任务 `cef858c26266` 状态为 `exited` |
| Flowable 服务 | 沙箱外默认端口检查返回 HTTP `502`，不能视为服务就绪 |
| AE score service | 已使用 `model_stage/nv_valid_server_real.py` + O2OA AE@1.0 模型启动并完成真实 smoke 在线 RPC 验证，`rpc_ok=True`，`rpc_fail_total=0` |
| GAN score service | `/tmp/nv_valid_gan.sock` 不存在；GAN RPC 未在线 |
| O2OA 凭据环境变量 | `O2OA_BASE`、`O2OA_USER`、`O2OA_PASS` 存在；`O2OA_TOKEN` 不存在 |
| O2OA token 获取 | 真实 smoke 通过环境变量注入授权信息；本报告不记录 token 明文 |

必要文件检查结果：

| 文件 | 结果 |
| --- | --- |
| `integration/fuzz_adapter.py` | 存在 |
| `integration/decision_engine.py` | 存在 |
| `integration/platform_profiles/o2oa_default.json` | 存在 |
| `integration/platform_profiles/flowable_v2.json` | 存在 |
| `docs/integration/examples/fuzz_submit_o2oa.json` | 存在 |
| `runner/templates/task_ae_default.json` | 存在 |
| `model_stage/models/sefanogan_ae_model.pt` | 存在 |
| `model_stage/models/sefanogan_ae_meta.json` | 存在 |
| `nv_body_valid.py` | 存在 |
| `docs/integration/examples/fuzz_submit_flowable.json` | 存在 |
| `model_stage/models/flowable_ae_v2_model.pt` | 存在 |
| `model_stage/models/flowable_ae_v2_meta.json` | 存在 |

## 3. 静态验证结果

| 检查项 | 结果 |
| --- | --- |
| `DecisionEngine` 是否存在 | 是，位于 `integration/decision_engine.py` |
| O2OA profile 是否包含 `decision` 字段 | 是 |
| Flowable profile 是否包含 `decision` 字段 | 是 |
| O2OA 是否启用 second stage | 是，`enable_second_stage=true`，`second_stage_type=gan` |
| Flowable 是否启用 second stage | 否，`enable_second_stage=false`，仅保留结构 |
| `fuzz_adapter` 是否注入 `NV_DECISION_PROFILE_PATH` | 是，dry-run launch command 已包含该环境变量 |
| `fuzz_adapter` 是否注入 `NV_DECISION_PROFILE_NAME` | 是 |
| `nv_body_valid.py` 是否进入 runtime decision | 是，已通过真实 AE score service 触发 score RPC，随后加载 profile 并调用 `DecisionEngine.decide()` |
| Flowable 脚本是否进入 runtime decision | 是，`scripts/run_flowable_process_start_compare.py` 在 AE score 后调用 `DecisionEngine.decide()` |
| Flowable 脚本语法检查 | 通过 |
| `run_flowable_process_start_compare.py` AST 路径检查 | 通过 |

O2OA dry-run：

- 命令：`python3 integration/fuzz_adapter.py submit --dry-run --request-json docs/integration/examples/fuzz_submit_o2oa.json`
- 结果：通过，`process_result=1`
- profile：`o2oa_default`
- template：`runner/templates/task_ae_default.json`
- 模型口径：`model_name=ae`，`threshold=1.0`，AE 模型与 meta 文件均存在
- 注入项：`NV_DECISION_PROFILE_PATH=/home/dministrator/AFLplusplus/integration/platform_profiles/o2oa_default.json`
- 决策结构：`t_low=0.8`，`t_high=1.5`，`enable_second_stage=true`，`second_stage_type=gan`

Flowable dry-run：

- 命令：`python3 integration/fuzz_adapter.py submit --dry-run --request-json docs/integration/examples/fuzz_submit_flowable.json`
- 结果：通过，`process_result=1`
- profile：`flowable_v2`
- template：`runner/templates/task_ae_flowable_eval_v2.json`
- 模型口径：`model_name=ae_flowable_v2`，`threshold=6.3`，Flowable AE v2 模型与 meta 文件均存在
- 注入项：`NV_DECISION_PROFILE_PATH=/home/dministrator/AFLplusplus/integration/platform_profiles/flowable_v2.json`
- 决策结构：`t_low=5.8`，`t_high=6.3`，`enable_second_stage=false`

`submit / query / report_query` 接口层检查：

- 本轮已完成 O2OA 真实 smoke，`task_id=cef858c26266`，状态为 `exited`。
- 报告文件：`runner/tasks/cef858c26266/report.json`。
- 20s 统计：`body_score_pass=81`，`body_score_reject=41`，即 `20s pass/reject=81/41`。
- AE score RPC 在真实 smoke 中在线参与，`rpc_ok=True`，`body_score_rpc_fail=0`，`rpc_fail_total=0`。
- 报告中的 `decision_summary` 已记录 `dur20_pass_reject=81/41` 与 `rpc_fail_total=0`。

AE score service runtime 检查：

- 启动命令：`SEFANOGAN_MODE=ae SEFANOGAN_MODEL_PATH=model_stage/models/sefanogan_ae_model.pt SEFANOGAN_AE_META_PATH=model_stage/models/sefanogan_ae_meta.json NV_VALID_SOCK=/tmp/nv_valid_real.sock python3 model_stage/nv_valid_server_real.py`
- 沙箱内启动因 socket bind 权限失败；沙箱外授权启动成功。
- 最小 socket RPC：通过，`ae_rpc_ok=True`，`ae_score_present=True`。
- `nv_body_valid.body_validate()` + `NV_DECISION_PROFILE_PATH=integration/platform_profiles/o2oa_default.json`：通过，`score_rpc_ok=True`，`decision=pass`，`stage=ae_low`。
- 正式 O2OA profile 阈值为 `t_low=0.8`、`t_high=1.5`；当前真实 smoke seed 分数未落入灰区，因此真实 smoke 未触发 second stage 是正常结果。
- 使用临时灰区 profile `/tmp/o2oa_decision_grey_probe.json` 完成定向 second stage 验证；该 profile 仅用于机制验证，不替代正式 O2OA 工作阈值。

## 4. 动态决策验证结果

真实 smoke 覆盖了 AE score RPC 在线参与与正式 profile 下的 AE 分区决策；定向灰区验证使用临时 profile 覆盖 second stage 与 rule fallback 路径。

| profile | 输入分区 | 结果 | stage | 说明 |
| --- | --- | --- | --- | --- |
| `o2oa_default` | `ae_score <= t_low`，score=`0.8` | `pass` | `ae_low` | AE 低风险直接通过 |
| `o2oa_default` | `ae_score >= t_high`，score=`1.5` | `reject` | `ae_high` | AE 高风险直接拒绝 |
| `o2oa_default` | `t_low < ae_score < t_high`，score=`1.15` | `pass` | `second_pass` | 已进入 second stage；GAN socket 不在线时走 rule fallback |
| `o2oa_default` | 灰区高风险样本 | `reject` | `second_reject` | 已进入 second stage；rule fallback 生效并拒绝 |
| `/tmp/o2oa_decision_grey_probe.json` | `0.5 < ae_score < 0.8`，score=`0.557637` | `pass` | `second_pass` | 临时灰区 profile 定向触发 second stage，`second_stage_source=rule_fallback`，`fallback_reason=gan_rpc_unavailable` |
| `/tmp/o2oa_decision_grey_probe.json` | `0.5 < ae_score < 0.8`，score=`0.611793` | `pass` | `second_pass` | 临时灰区 profile 定向触发 second stage，`second_stage_source=rule_fallback`，`fallback_reason=gan_rpc_unavailable` |
| `flowable_v2` | `ae_score <= t_low`，score=`5.8` | `pass` | `ae_low` | AE 低风险直接通过 |
| `flowable_v2` | `ae_score >= t_high`，score=`6.3` | `reject` | `ae_high` | AE 高风险直接拒绝 |
| `flowable_v2` | `t_low < ae_score < t_high`，score=`6.05` | `reject` | `fallback` | Flowable 当前未启用 second stage |

GAN / fallback 结论：

- GAN RPC：未在线，`/tmp/nv_valid_gan.sock` 不存在。
- Rule fallback：验证通过。临时灰区 profile 下，分数 `0.557637` 与 `0.611793` 均进入灰区并触发 `second_pass`，GAN socket 不在线时按设计进入 `second_stage_source=rule_fallback`，`fallback_reason=gan_rpc_unavailable`。
- AE score service：本轮已在真实 smoke 中在线参与，`rpc_ok=True`，`rpc_fail_total=0`。正式 profile 下当前 seed 未覆盖灰区，未触发 second stage 属于预期结果。

## 5. 真实 smoke 结果

### O2OA

真实 O2OA smoke 已完成。

结果：

- `task_id=cef858c26266`
- `status=exited`
- 报告文件：`runner/tasks/cef858c26266/report.json`
- 20s pass/reject：`81/41`
- AE score RPC 在线参与，`rpc_ok=True`，`rpc_fail_total=0`
- 正式 profile 为 `o2oa_default`，`t_low=0.8`、`t_high=1.5`

结论：

- 当前真实 smoke seed 分数未落入正式 profile 灰区，因此未触发 second stage 是正常结果。
- 使用临时灰区 profile `/tmp/o2oa_decision_grey_probe.json` 已完成定向 second stage 验证；分数 `0.557637` 与 `0.611793` 均进入灰区，DecisionEngine 输出 `decision=pass`、`stage=second_pass`、`second_stage_source=rule_fallback`、`fallback_reason=gan_rpc_unavailable`。
- 临时灰区 profile 仅用于机制验证，不替代正式 O2OA 工作阈值。

### Flowable

真实 Flowable smoke 未执行。

阻塞原因：

- Flowable 默认端口检查返回 HTTP `502`，不能视为服务就绪；
- Flowable 专用 AE score service 未启动；本轮只按 Flowable 服务不可用分支完成静态、dry-run 与脚本检查；
- Flowable profile 当前 `enable_second_stage=false`，只保留 decision 结构，不启用 second stage。

本轮没有生成新的 Flowable `task_id`、stdout、stderr 或 report。

## 动态冗余终验证据

本节记录 2026-04-30 终验补充证据，重点验证正式 O2OA profile 的 runtime 决策日志，以及临时灰区 profile 的 second stage 触发路径。本节不包含任何 token、cookie、请求正文、账号或密码明文。

### 正式 O2OA profile runtime 证据

- 正式 profile：`integration/platform_profiles/o2oa_default.json`，阈值保持 `t_low=0.8`、`t_high=1.5`，未修改正式模型路线。
- 既有真实 O2OA smoke：`task_id=cef858c26266`，`status=exited`，20s pass/reject=`81/41`，`rpc_fail_total=0`，report=`runner/tasks/cef858c26266/report.json`。该 run 的 `summary_dur20.csv` 记录 `last_http_code=200`，可作为真实 O2OA 业务接口 smoke 证据。
- 本轮正式日志型 runtime run：`task_id=c506e3dd9815`，`status=exited`，report=`runner/tasks/c506e3dd9815/report.json`，stdout=`runner/runs/c506e3dd9815/stdout.log`，stderr=`runner/runs/c506e3dd9815/stderr.log`，stats=`runner/runs/c506e3dd9815/body_valid_stats_dur20.json`。
- 本轮日志型 run 的 20s 统计：pass/reject=`79/40`，`body_score_rpc_ok=119`，`body_score_rpc_fail=0`，`rpc_fail_total=0`。
- `BODY_DECISION_DBG` 已在本轮正式 runtime stderr 中出现，共 `119` 条。
- 正式 profile stage 分布：`ae_low=79`，`ae_high=40`，`fallback=0`，`second_pass=0`，`second_reject=0`。
- 结论：补证前正式 seed 未覆盖灰区，因此该 run 未命中 second stage 是正常结果，不作为失败处理。
- 说明：本轮日志型 run 用于补充 `BODY_DECISION_DBG` runtime 证据；其 HTTP 末次状态为 `401`，因此真实业务接口成功仍以 `cef858c26266` 的 HTTP `200` smoke 作为证据。

### 临时灰区 profile runtime 证据

- 临时 profile：`/tmp/o2oa_decision_grey_probe.json`，仅用于机制验证，未修改 `integration/platform_profiles/o2oa_default.json`。
- 临时灰区阈值：`t_low=0.5`，`t_high=0.8`。
- 最小 runtime 路径：调用 `nv_body_valid.body_validate()`，使用真实 AE score RPC `unix:///tmp/nv_valid_real.sock`，日志落盘于 `/tmp/o2oa_grey_probe_runtime.log`。
- 灰区样本：`fuzz_border_2.json` 与 `seed_border_3.json` 当前 AE score 均为 `0.561212`，进入临时灰区。
- `BODY_DECISION_DBG` 已出现，输出包含 `stage=second_pass`、`second_stage_source=rule_fallback`、`fallback_reason=gan_rpc_unavailable`。
- 结论：灰区样本能够触发 second stage；GAN socket 未在线时，DecisionEngine 按设计进入 rule fallback。

### 终验结论

- 正式 O2OA smoke 已完成：是，`cef858c26266` 提供真实业务接口 smoke 证据，`c506e3dd9815` 补充正式 profile runtime debug 日志证据。
- `BODY_DECISION_DBG` 是否在 runtime 中出现：是，正式 profile 日志型 run 与临时灰区最小 runtime 路径均已出现。
- 正式 profile 是否命中灰区：补证前未命中；新增正式灰区样本后的补充证据见下节。
- 临时灰区 profile 是否触发 second stage：是，触发 `second_pass`。
- rule fallback 是否生效：是，GAN socket 未在线时进入 `second_stage_source=rule_fallback`，原因是 `fallback_reason=gan_rpc_unavailable`。
- GAN 在线路径是否验证：否，不能表述为 AE+GAN 在线验证通过。
- Flowable second stage 是否启用：否，Flowable profile 当前 `enable_second_stage=false`，不能表述为 Flowable 动态冗余已完成。
- 当前是否可以称为“轻量级动态异构冗余机制”：可以，但范围限定为 O2OA AE 分区决策、灰区 second stage 调度机制，以及 GAN 不在线时的 rule fallback；临时灰区 profile 只作为机制验证，不替代正式 O2OA 工作阈值。

## 正式灰区样本补证

本节记录 2026-04-30 对正式 O2OA profile 灰区 `0.8 < ae_score < 1.5` 的补充验证。验证过程中未修改 `integration/platform_profiles/o2oa_default.json`，正式阈值保持 `t_low=0.8`、`t_high=1.5`；未修改 AFL++ 主链，未扩展 Flowable，未启动 GAN。

新增灰区样本：

| 样本 | AE score | 是否落入正式灰区 | 说明 |
| --- | --- | --- | --- |
| `in/o2oa_body_model_compare/grey_probe_0.json` | `1.410657` | 是 | 由既有 `fuzz_border_2.json` 轻量变异得到 |
| `in/o2oa_body_model_compare/grey_probe_1.json` | `1.050980` | 是 | 由既有 `fuzz_border_2.json` 轻量变异得到 |
| `in/o2oa_body_model_compare/grey_probe_2.json` | `1.035954` | 是 | 由既有 `fuzz_border_2.json` 轻量变异得到 |

正式 profile 最小路径验证：

- 使用 `NV_DECISION_PROFILE_PATH=integration/platform_profiles/o2oa_default.json` 与真实 AE score service `unix:///tmp/nv_valid_real.sock` 调用 `nv_body_valid.body_validate()`。
- 3 个 `grey_probe` 样本均输出 `BODY_DECISION_DBG`。
- 3 个样本均触发 `stage=second_pass`，并出现 `second_stage_source=rule_fallback`、`fallback_reason=gan_rpc_unavailable`。
- 该结果说明正式 profile 的灰区调度逻辑可触发 second stage；GAN socket 未在线时按设计进入 rule fallback。

正式 profile 短时 smoke：

- 任务：`task_id=c8b0d1c25e45`，`status=exited`。
- 报告：`runner/tasks/c8b0d1c25e45/report.json`。
- 日志：`runner/runs/c8b0d1c25e45/stdout.log`、`runner/runs/c8b0d1c25e45/stderr.log`。
- 统计：`runner/runs/c8b0d1c25e45/body_valid_stats_dur20.json`。
- 20s pass/reject：`104/36`。
- `body_score_rpc_ok=140`，`body_score_rpc_fail=0`，`rpc_fail_total=0`。
- `BODY_DECISION_DBG` 共 `140` 条。
- stage 分布：`ae_low=79`，`ae_high=36`，`fallback=0`，`second_pass=25`，`second_reject=0`。
- `second_stage_source=rule_fallback` 出现 `25` 次。
- `fallback_reason=gan_rpc_unavailable` 出现 `25` 次。

结论：

- 正式 O2OA profile 在新增正式灰区样本后已触发 second stage。
- rule fallback 已在正式 profile runtime smoke 中生效。
- GAN 在线路径仍未验证，不能表述为 AE+GAN 在线验证通过。
- Flowable second stage 仍未启用，不能表述为 Flowable 动态冗余已完成。
- 本轮补证增强了“轻量级动态异构冗余机制”的证据：正式 O2OA profile 下已覆盖 AE 低风险直通、AE 高风险拒绝、灰区 second stage 调度、GAN 不在线时 rule fallback。
- 说明：本轮灰区 smoke 的末次 HTTP 状态为 `401`，因此真实业务接口 HTTP `200` 证据仍以 `cef858c26266` 为准；本轮新增证据聚焦正式 profile 的 runtime decision 与 second stage 触发。

## 证据固化与最终收口

证据已固化到目录：`docs/review/evidence/o2oa_dynamic_redundancy/`。

归档文件：

| 文件 | 说明 |
| --- | --- |
| `grey_probe_0.json` | 正式灰区样本，AE score=`1.410657` |
| `grey_probe_1.json` | 正式灰区样本，AE score=`1.050980` |
| `grey_probe_2.json` | 正式灰区样本，AE score=`1.035954` |
| `report_c8b0d1c25e45.json` | 正式 profile 灰区 smoke 报告 |
| `summary_dur20_c8b0d1c25e45.csv` | 正式 profile 灰区 smoke 20s 汇总 |
| `body_valid_stats_dur20_c8b0d1c25e45.json` | 正式 profile 灰区 smoke body valid 统计 |
| `decision_debug_excerpt.log` | 从 `c8b0d1c25e45` stderr 中提取的 `BODY_DECISION_DBG` / `second_pass` / `rule_fallback` 脱敏摘录 |
| `report_cef858c26266_http200.json` | HTTP `200` 真实业务 smoke 报告 |

`c8b0d1c25e45` 证明：

- 使用未修改的正式 `o2oa_default` profile，正式阈值仍为 `t_low=0.8`、`t_high=1.5`。
- AE score RPC 在线参与，`rpc_fail_total=0`。
- 新增正式灰区样本触发 runtime `second_pass`。
- `second_stage_source=rule_fallback` 与 `fallback_reason=gan_rpc_unavailable` 均出现 `25` 次。
- 该任务证明正式 profile 下灰区 second stage 调度与 rule fallback 机制可运行。

`cef858c26266` 证明：

- O2OA 真实业务 smoke 已完成，`status=exited`。
- 20s pass/reject=`81/41`，`rpc_fail_total=0`。
- 该 run 的 20s summary 记录 HTTP `200`，用于支撑真实 O2OA 业务接口可达与 smoke 成功。

一体化 smoke：

- 已完成新的“一体化 HTTP `200` + `second_pass`” smoke。
- 任务：`task_id=310394c0b2d4`，`status=exited`。
- 报告：`runner/tasks/310394c0b2d4/report.json`。
- 运行产物：`runner/runs/310394c0b2d4/summary_dur20.csv`、`runner/runs/310394c0b2d4/body_valid_stats_dur20.json`、`runner/runs/310394c0b2d4/stderr.log`。
- 20s pass/reject：`97/40`。
- HTTP 结果：`summary_dur20.csv` 记录 `last_http_code=200`。
- AE score RPC：`body_score_rpc_ok=137`，`body_score_rpc_fail=0`，`rpc_fail_total=0`。
- Runtime debug：`BODY_DECISION_DBG=137`。
- Stage 分布：`ae_low=75`，`ae_high=40`，`fallback=0`，`second_pass=22`，`second_reject=0`。
- Rule fallback：`second_stage_source=rule_fallback` 出现 `22` 次，`fallback_reason=gan_rpc_unavailable` 出现 `22` 次。
- 结论：`310394c0b2d4` 是同一 task 内同时满足 HTTP `200`、`rpc_fail_total=0`、`second_pass>0`、`BODY_DECISION_DBG` 出现的一体化 smoke 证据。

最终收口结论：

- 当前证据支持“O2OA 轻量级动态异构冗余机制已完成”的工程结论。
- 结论范围限定为：AE 一阶段分区、正式灰区样本触发 second stage、GAN socket 未在线时进入 rule fallback、真实 O2OA HTTP `200` smoke 已存在。
- 不能声称 GAN 在线路径已验证。
- 不能声称 Flowable 动态冗余已完成。
- 在获得同一任务同时满足 HTTP `200`、`rpc_fail_total=0`、`second_pass>0` 之前，必须说明 HTTP `200` 与 second stage 证据来自分离任务。

## 6. 最终判断

| 判断项 | 结论 |
| --- | --- |
| 是否实现 runtime decision | 是。`nv_body_valid.py` 已通过真实 AE score RPC 后进入 `DecisionEngine`；Flowable 对比脚本也在 AE score 后进入 `DecisionEngine`；dry-run 已确认 profile path 注入 |
| 是否存在冗余路径 | 是。O2OA profile 具备 AE 一阶段 + GAN RPC / rule fallback 二阶段路径 |
| 是否存在异构路径 | 是。O2OA 中 AE、GAN 与 rule fallback 属于不同判定机制；本轮只验证到 rule fallback，GAN 在线未验证 |
| 是否存在动态调度 | 是。O2OA 根据 `t_low / t_high` 在运行时只对灰区样本触发 second stage；正式灰区样本补证已触发 `second_pass` |
| 是否可以称为“轻量级动态异构冗余机制” | 是。真实 O2OA smoke 已完成，AE 分区决策有效；正式灰区样本已验证灰区样本可触发 second stage，并在 GAN socket 未在线时按设计进入 rule fallback |
| 是否完成真实 O2OA smoke | 是，`task_id=cef858c26266`，`status=exited`，20s pass/reject=`81/41`，`rpc_fail_total=0` |
| 是否完成真实 Flowable smoke | 否 |
| `submit / query / report_query` 是否完成本轮真实闭环 | O2OA 真实 smoke 与 report 已完成；Flowable 真实 smoke 未完成 |

## 7. 限制说明

- GAN 未在线，本轮不能声称 GAN RPC 在线路径真实运行成功，只能说明 GAN 不可用时的 rule fallback 路径验证通过。
- 补证前正式 O2OA seed 未覆盖灰区；本轮已新增 `grey_probe_*.json` 正式灰区样本并在正式 profile 下触发 second stage。
- 临时灰区 profile `/tmp/o2oa_decision_grey_probe.json` 仅用于机制验证，不替代正式 O2OA 工作阈值 `t_low=0.8`、`t_high=1.5`。
- Docker CLI 与 Docker Compose 在当前 WSL 发行版不可用；仓库内未发现可直接启动 O2OA / Flowable 的 compose 文件。
- Flowable 当前 profile 保留 `decision` 结构，但 `enable_second_stage=false`，不能表述为 Flowable second stage 已启用。
- Flowable 真实 smoke 未完成，不能表述为 Flowable 端到端 second stage 已验证。
- 本轮验证支持“轻量级动态异构冗余机制已实现”的工程结论，但范围限定为 O2OA AE 分区 + 正式灰区 second stage 调度 + GAN 不在线时 rule fallback；GAN 在线路径与 Flowable second stage 仍未验证。
