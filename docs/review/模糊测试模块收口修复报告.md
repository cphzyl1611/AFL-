# 模糊测试模块收口修复报告

生成日期：2026-05-08

## 1. 本次修复的问题

### 1.1 valid/invalid 计数重复自增

- 修改文件：`src/afl-fuzz-run.c`
- 修复内容：
  - 新增统一入口 `nv_account_valid()`
  - 保留 `nv_account_invalid()` 作为 invalid 唯一入口
  - 移除 trim 路径中 `nv_invalid_cnt++` 后又调用 `nv_account_invalid()` 的重复计数
  - 移除 trim 路径中连续两次 `nv_valid_cnt++` 的重复计数
  - 移除主执行路径中 `nv_invalid_cnt++` 后又调用 `nv_account_invalid()` 的重复计数
- 保持不变：
  - `fuzz_run_target()` 中 `nv_total_valid_exec` 语义不变
  - `max_test_cases` 仍按 `nv_total_valid_exec` 停止
- 验证命令：
  - `make -j2 afl-fuzz`
  - 启用 `NV_TASK_PATH` 的 2 秒短任务：`AFL_NO_UI=1 AFL_SKIP_CPUFREQ=1 AFL_I_DONT_CARE_ABOUT_MISSING_CRASHES=1 NV_TASK_PATH=/tmp/nv_count_step1_2/task.json ./afl-fuzz -n -d -i /tmp/nv_count_step1_2/in -o /tmp/nv_count_step1_2/out -V 2 -- /bin/cat @@`
- 验证结果：
  - 编译通过
  - `nv_valid_cnt=8`
  - `nv_invalid_cnt=4`
  - `nv_invalid_parse_cnt + nv_invalid_rule_cnt + nv_invalid_rpc_fail_cnt = 4`
  - `nv_total_valid_exec=8`
  - `max_test_cases=8` 生效

### 1.2 plot_data header/value 顺序错位

- 修改文件：`src/afl-fuzz-init.c`、`src/afl-fuzz-stats.c`
- 修复内容：
  - 将 header 顺序调整为与实际写入顺序一致：MAB pos、MAB mean、MAB pulls、HTTP status、ncov、Err/Rec
  - 末尾补充 `nv_valid_cnt`、`nv_invalid_cnt`、`nv_err_rate`、`nv_rec_rate`
- 验证命令：
  - `make -j2 afl-fuzz`
  - 短任务生成 `plot_data`
  - Python 检查 header/value 列数与关键列名映射
- 验证结果：
  - `plot_data` header 数量：35
  - `plot_data` value 数量：35
  - 抽查字段 `nv_valid_cnt`、`nv_invalid_cnt`、`nv_err_rate`、`nv_rec_rate`、`nv_mab_a0_pulls`、`nv_http_ok` 均按列名正确取值

### 1.3 eval_report.json 与 summary/report 字段语义统一

- 修改文件：
  - `src/afl-fuzz-stats.c`
  - `runner/fuzz_test_runner.py`
  - `scripts/run_cms_body_valid_compare_real.sh`
  - `scripts/run_cms_body_valid_compare_real_rulescore_only.sh`
  - `scripts/run_flowable_process_start_compare.py`
  - `docs/integration/模糊测试模块调用说明.md`
- 修复内容：
  - `eval_report.json` 的 `task` 增加 `source`
    - `nv_task_path`：启用了 `NV_TASK_PATH`
    - `harness_only`：未启用 C 侧 task metadata，任务上下文来自 harness/report 产物
    - `adapter_demo`：runner/adapter 集成 demo
  - O2OA summary 增加 `summary_source=aflpp_harness`
  - Flowable summary 增加 `summary_source=python_static_loop`
  - runner report 增加 `metric_sources` 和 `integration_boundary`
  - report 选择 summary 时优先选择 `rule_score` 行，避免误读 baseline 行
- 验证命令：
  - `bash -n scripts/run_cms_body_valid_compare_real.sh scripts/run_cms_body_valid_compare_real_rulescore_only.sh`
  - `python3 -m py_compile runner/fuzz_test_runner.py scripts/run_flowable_process_start_compare.py`
  - `python3 runner/fuzz_test_runner.py report --task-id 63306fbe9ebd`
  - `python3 runner/fuzz_test_runner.py report --task-id 779145c12c25`
  - 无 `NV_TASK_PATH` 的 1 秒 AFL++ 短任务检查 `eval_report.json`
- 验证结果：
  - 语法检查通过
  - O2OA report：`summary_source=aflpp_harness`，`execution_scope=o2oa_aflpp_body_harness`
  - Flowable report：`summary_source=python_static_loop`，`execution_scope=flowable_min_calibration`
  - 无 `NV_TASK_PATH` 时：`eval_report.json` 写出 `task.source=harness_only`

### 1.4 profile/source-of-truth 与文档口径统一

- 修改文件：
  - `integration/platform_profiles/o2oa_default.json`
  - `integration/platform_profiles/flowable_v2.json`
  - `runner/templates/*.json`
  - `docs/final_results/model_route_final_decision.md`
  - `docs/final_delivery/fuzz_component_release/README.md`
  - `docs/最终交付说明.md`
- 修复内容：
  - O2OA 明确为 AE@1.0 默认验收主线，GAN online 仅为二阶段增强
  - Flowable 明确为 Flowable-AE v2@6.3 + `flowable_rule_v1` 最小二阶段验证，`is_mainline=false`
  - Flowable runner template 统一使用 `unix:///tmp/nv_valid_flowable.sock`
  - O2OA GAN research template 统一使用 `unix:///tmp/nv_valid_gan.sock`
  - profile 增加模型 sha256/source-of-truth
- source-of-truth：
  - O2OA AE model：`model_stage/models/sefanogan_ae_model.pt`，sha256=`5de67e66e28af65fdf6adb5c506c91148fcc356accd49113c267896bbd69d3dc`
  - O2OA AE meta：`model_stage/models/sefanogan_ae_meta.json`，sha256=`208daf66d82873dec8c19923d9c0a5a6b82ca36c512de52b0ff16b24ecb7acf2`
  - O2OA GAN online model：`model_stage/models/sefanogan_gan_model.pt`，sha256=`205a6a20a499a39b994a7ea5a770d0f701fcaba37cd9328da7e71d4aaf856624`
  - O2OA GAN online meta：`model_stage/models/sefanogan_gan_meta.json`，sha256=`87d189dde54ac578d170850d090032d3268887517d259ad5fc96855a15b9c495`
  - Flowable AE v2 model：`model_stage/models/flowable_ae_v2_model.pt`，sha256=`72780ccf24401f20e5ae7127955c0b192ec6cbd5d88099ce3c4dc9ab03053bee`
  - Flowable AE v2 meta：`model_stage/models/flowable_ae_v2_meta.json`，sha256=`b3728ec4340a18b6abdeffdce3213778bde4cedbaf6ec048efd497866d510e80`
- 验证命令：
  - `python3 - <<'PY' ... json.load(...) ... PY`
  - 使用禁用口径关键词执行 `rg -n` 检查
  - `rg -n "Flowable.*nv_valid_real|flowable.*nv_valid_real|task_.*flowable.*nv_valid_real|nv_valid_real" runner/templates/task_*flowable*.json scripts/run_flowable_process_start_compare.py integration/platform_profiles/flowable_v2.json`
- 验证结果：
  - 14 个 profile/template JSON 均可解析
  - 禁用口径 grep 命中均处于“未完成/不要表述/不 claim”语境
  - Flowable profile/template 未再混用 `nv_valid_real.sock`

### 1.5 submit/query/stop/report_query 最小集成验证

- 修改/新增文件：
  - `integration/fuzz_adapter.py`
  - `integration/fuzz_adapter_demo_worker.py`
  - `runner/fuzz_test_runner.py`
  - `docs/integration/examples/fuzz_submit_demo.json`
  - `docs/integration/模糊测试模块调用说明.md`
- 修复内容：
  - 新增 adapter demo 模式：只生成 synthetic `fuzzer_stats`、`eval_report.json`、`summary_dur20.csv`
  - `query` 能读取 `fuzzer_stats` 与 `eval_report.json`
  - `stop` 能对 demo 任务发送信号并使状态转为 stopped
  - `report_query` 能刷新并列出 report/artifacts
  - 文档明确：这是 runner/adapter 级平台语义，不是 HTTP/RPC 网关服务
- 验证命令：
  - `python3 -m py_compile integration/fuzz_adapter.py integration/fuzz_adapter_demo_worker.py runner/fuzz_test_runner.py`
  - `python3 integration/fuzz_adapter.py submit --request-json docs/integration/examples/fuzz_submit_demo.json`
  - `python3 integration/fuzz_adapter.py query --json '{"task_id":"fuzz-task-ae8b5976b468"}'`
  - `python3 integration/fuzz_adapter.py stop --json '{"task_id":"fuzz-task-ae8b5976b468"}'`
  - `python3 integration/fuzz_adapter.py report_query --json '{"task_id":"fuzz-task-ae8b5976b468"}'`
- 验证结果：
  - `submit` 返回 `process_result=1`，生成 `task_id=fuzz-task-ae8b5976b468`
  - `query` 返回 `status=running`，`metric_sources.eval_report=adapter_demo`
  - `stop` 返回 `signal_sent=true`
  - stop 后再次 `query` 返回 `status=stopped`，`pid_alive=false`
  - `report_query` 返回 `total_count=1`，报告包含 `fuzzer_stats`、`eval_report.json`、`summary_dur20.csv`

## 2. 没有修复的问题

以下问题属于后续产品化或总项目集成范围，本次收口修复不扩展：

1. 未新增完整 HTTP/RPC 网关服务；当前仍是 runner/adapter 级 `submit/query/stop/report_query` 任务语义。
2. 未把 Flowable 扩展为完整 AFL++ mutation 主链；当前仍是 AE+`flowable_rule_v1` 最小二阶段验证。
3. 未完成 Flowable-GAN；当前 Flowable 不使用 GAN 二阶段。
4. 未实现完整拟态系统级执行体调度、自愈、重构机制。
5. 未重跑 O2OA/Flowable 长时真实服务实验；真实环境端到端结果仍需在总集成环境运行验证。
6. 未处理 `src/afl-fuzz-one.c` 中历史 MAB 重复/未用变量问题；该问题不影响本次统计收口主线，但建议后续单独小 commit 处理。

## 3. 当前可以对老师说什么

1. 模糊测试模块已经完成阶段性收口修复，统计可信度和报告来源说明已补齐。
2. O2OA 可作为验收主线：AE@1.0 是默认验收模型，GAN online 是有效性验证链路中的二阶段增强。
3. Flowable 可作为第二平台支线：Flowable-AE v2@6.3 + `flowable_rule_v1` 最小二阶段验证已形成 profile、模板和报告口径。
4. 动态异构冗余当前完成的是模糊测试有效性验证链路中的二阶段判定机制。
5. `submit/query/stop/report_query` 已完成 runner/adapter 级最小集成验证，可进入总项目联调。

## 4. 当前绝对不能说什么

1. 不能说 Flowable-GAN 已完成。
2. 不能说 Flowable 已完成完整 AFL++ mutation 主链。
3. 不能说 GAN 已替代 AE 成为默认验收模型。
4. 不能说已经完成完整拟态系统级动态异构冗余执行体调度、自愈或重构。
5. 不能把 adapter demo 的 synthetic 统计当作真实模糊测试效果证据。
6. 不能把 runner/adapter 级任务语义表述为已经部署完成的 HTTP/RPC 平台网关服务。

## 5. 最终结论

本次修复已完成统计计数、plot_data、eval_report/report schema、profile/source-of-truth、文档口径和最小 runner/adapter 集成验证的收口工作。模糊测试模块允许阶段性收口，允许进入总项目集成联调。
