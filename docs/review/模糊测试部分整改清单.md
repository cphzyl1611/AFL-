# 模糊测试部分整改清单

依据文件：`docs/review/模糊测试部分终审报告.md`  
整改原则：只处理证据充分、风险可控、无需猜测的事项；涉及模型权重、缺 source-of-truth 的事项不伪造、不覆盖、不自动补权重。

| 风险编号 | 风险描述 | 证据路径 | 是否关键 | 是否可自动整改 | 整改策略 | 是否需要人工确认 | 当前状态 |
|---|---|---|---|---|---|---|---|
| RISK-01 | O2OA 默认 AE 模型文件被覆盖或与文档口径不一致。`sefanogan_ae_model.pt` 与 `flowable_ae_v3_model.pt` 哈希相同，AE meta 也呈 Flowable v3 特征。 | `model_stage/models/sefanogan_ae_model.pt`；`model_stage/models/flowable_ae_v3_model.pt`；`model_stage/models/sefanogan_ae_meta.json`；终审报告 RISK-01 | 是 | 否 | 不自动覆盖模型。需要人工提供或确认 O2OA AE@1.0 的最终 `.pt` 与 meta source-of-truth，再替换或补齐。 | 是 | 阻塞 |
| RISK-02 | 交付目录缺 O2OA 模型权重，`docs/final_delivery/fuzz_component_release/` 未包含 O2OA AE/GAN `.pt` 权重。 | `docs/final_delivery/fuzz_component_release/`；终审报告 RISK-02 | 是 | 否 | 不自动创建或复制 `.pt`。GAN 权重 source 可部分定位，但 O2OA AE 权重缺最终确认，故不做不完整交付修复。 | 是 | 阻塞 |
| RISK-03 | GAN 交付 meta 与最终口径不一致，交付目录 alpha/beta 为 0.85/0.15，最终口径为 0.90/0.10。 | `docs/final_delivery/fuzz_component_release/sefanogan_gan_meta.json`；`model_stage/models/sefanogan_gan_meta.json`；`docs/final_results/batch_compare_runs_gan_v2_t17_ab9010/`；终审报告 RISK-03 | 是 | 是 | 以 `model_stage/models/sefanogan_gan_meta.json` 和 t17_ab9010 run3 结果为 source-of-truth，同步交付 meta 中 alpha/beta 与统计值；同时补充模型路线文档最终口径。 | 否 | 已整改 |
| RISK-04 | 快速使用说明 Markdown 代码块未闭合，且历史 Flowable v1/v2 口径混杂。 | `docs/final_delivery/fuzz_component_release/fuzz_component_quickstart.md`；终审报告 RISK-04 | 中 | 是 | 修复 AE 启动代码块；把 Flowable v1@12.4 标明为历史记录；明确最终 Flowable 口径为 v2@6.3。 | 否 | 已整改 |
| RISK-05 | O2OA 场景与设计文件表述存在偏差，当前证据集中在 `cms_doc_list` 等 O2OA 数据接口，不应扩大为完整电子公文创建/保存/更新全流程覆盖。 | `docs/final_delivery/fuzz_component_release/fuzz_component_quickstart.md`；`docs/final_delivery/fuzz_component_release/README.md`；`docs/final_delivery/fuzz_component_release/fuzz_project_final_closure.md`；终审报告 RISK-05 | 中高 | 是 | 在交付 README、quickstart、收口总结中补充 O2OA 场景口径边界，明确“数据接口阶段验收口径”和“不完整覆盖全业务流程”的限制。 | 否 | 已整改 |
| RISK-07 | Flowable 第二平台容易被过度表述为完整迁移；当前实际是静态混合数据集评分和 REST POST 的最小校准验证，不是完整 AFL++ 变异主链。 | `scripts/run_flowable_process_start_compare.py`；`runner/templates/task_ae_flowable_eval_v2.json`；`docs/final_delivery/fuzz_component_release/fuzz_project_final_closure.md`；终审报告 RISK-07 | 中 | 是 | 在交付文档和 Flowable 模板 notes 中明确“最小校准验证”“静态混合数据集”“不等同完整 AFL++ 变异模糊测试主链”。 | 否 | 已整改 |

## 不在本次自动整改范围内的风险

RISK-06、RISK-08、RISK-09 不属于本次用户指定重点项。本次未修改 `src/`、`include/`、`model_stage/*.py` 或 AFL++ 主链代码，未重跑实验，未安装依赖。
